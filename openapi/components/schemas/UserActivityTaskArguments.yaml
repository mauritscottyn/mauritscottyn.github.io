type: object
properties:
  query_start:
    type: string
    description: >-
      The day/timestamp you want the start of your data. The task will take all
      the users since this moment and compute their journey. "auto" means since
      last run.
    default: auto
